#ifndef HUMIDITY_H
#define HUMIDITY_H

void delay1 (int);
double getHumidity();

//typedef struct hum_struct {
	//float		hum;
	//pthread_t	Thread;
	//pthread_mutex_t lock;

//} HumStruct;


//int HumInit (HumStruct *Temp);
//int TempStart (void);
//int TempStop (TempStruct *Temp);

#endif
